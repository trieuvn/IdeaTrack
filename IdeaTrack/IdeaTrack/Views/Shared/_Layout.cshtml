<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>UEF Initiative Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: #f4f6fb;
            color: #1f2937;
        }

        .app {
            display: grid;
            grid-template-columns: 80px 1fr;
            min-height: 100vh;
        }
        /* Sidebar */
        .sidebar {
            background: #0f172a;
            color: #fff;
            padding: 20px 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 24px;
        }

            .sidebar .logo {
                width: 42px;
                height: 42px;
                border-radius: 12px;
                background: #6366f1;
                display: flex;
                align-items: center;
                justify-content: center;
                font-weight: 700;
            }

            .sidebar .nav i {
                font-size: 20px;
                opacity: 0.7;
                cursor: pointer;
            }
        /* Main */
        .main {
            padding: 24px 32px;
        }

        .topbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 24px;
        }

        .search {
            width: 360px;
            padding: 10px 14px;
            border-radius: 10px;
            border: 1px solid #e5e7eb;
        }

        .btn-primary {
            background: #6366f1;
            color: #fff;
            padding: 10px 16px;
            border-radius: 10px;
            border: none;
            font-weight: 600;
            cursor: pointer;
        }
        /* Welcome */
        .welcome {
            display: grid;
            grid-template-columns: 2fr 1fr;
            gap: 20px;
            margin-bottom: 24px;
        }

        .welcome-card {
            background: linear-gradient(135deg,#6366f1,#818cf8);
            color: #fff;
            padding: 28px;
            border-radius: 20px;
        }

            .welcome-card h2 {
                margin-bottom: 8px;
            }

            .welcome-card p {
                opacity: 0.9;
                margin-bottom: 16px;
            }

            .welcome-card .actions button {
                margin-right: 10px;
                border-radius: 10px;
                padding: 8px 14px;
                border: none;
                cursor: pointer;
                font-weight: 500;
            }

        .btn-light {
            background: #fff;
            color: #4f46e5;
        }

        .btn-outline {
            background: rgba(255,255,255,0.2);
            color: #fff;
        }

        .overview {
            background: #111827;
            color: #fff;
            border-radius: 20px;
            padding: 20px;
        }

            .overview h4 {
                margin-bottom: 16px;
            }

        .overview-item {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
            opacity: 0.9;
        }
        /* Metrics */
        .metrics {
            display: grid;
            grid-template-columns: repeat(4,1fr);
            gap: 16px;
            margin-bottom: 24px;
        }

        .metric {
            padding: 20px;
            border-radius: 16px;
            color: #fff;
        }

        .blue {
            background: #06b6d4;
        }

        .orange {
            background: #fb923c;
        }

        .green {
            background: #34d399;
        }

        .purple {
            background: #8b5cf6;
        }

        .metric h3 {
            font-size: 28px;
            margin-top: 6px;
        }
        /* Table */
        .card {
            background: #fff;
            border-radius: 16px;
            padding: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        th, td {
            text-align: left;
            padding: 12px 8px;
            border-bottom: 1px solid #e5e7eb;
            font-size: 14px;
        }

        .badge {
            padding: 4px 10px;
            border-radius: 999px;
            font-size: 12px;
            font-weight: 600;
        }

            .badge.review {
                background: #facc15;
            }

            .badge.approved {
                background: #4ade80;
            }
    </style>
</head>
<body>
    <div class="app">
        <aside class="sidebar">
            <div class="logo d-flex align-items-center justify-content-center">UEF</div>
            <div class="logo d-flex align-items-center justify-content-center">
                <i class="bi bi-grid fs-4 text-white"></i>
            </div>
            <div class="nav"><i class="bi bi-bar-chart fs-5"></i></div>
            <div class="nav"><i class="bi bi-file-earmark-text fs-5"></i></div>
            <div class="nav"><i class="bi bi-plus-circle fs-5"></i></div>
            <div class="nav"><i class="bi bi-gear fs-5"></i></div>
        </aside>

        <main class="main">
            @RenderBody()
        </main>
    </div>

    <script>
        // Placeholder JS for future interactions
        console.log('Dashboard loaded');
    </script>
</body>
</html>
