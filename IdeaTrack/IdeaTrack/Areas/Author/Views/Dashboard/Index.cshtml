@{
    ViewData["Title"] = "Overview";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="fw-bold text-dark">My Initiatives</h2>
        <a asp-controller="Initiative" asp-action="Create" class="btn btn-primary d-flex align-items-center gap-2">
            <span class="material-icons">add_circle</span>
            Create New Initiative
        </a>
    </div>

    <!-- Stats Cards -->
    <div class="row g-4 mb-4">
        <!-- Drafts -->
        <div class="col-md-3">
            <div class="card border-0 shadow-sm h-100 dash-card">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <div class="icon-box bg-secondary bg-opacity-10 text-secondary rounded-circle p-3">
                            <span class="material-icons">edit_note</span>
                        </div>
                        <span class="badge bg-secondary rounded-pill">Drafts</span>
                    </div>
                    <h3 class="fw-bold mb-1">@ViewBag.DraftCount</h3>
                    <p class="text-muted small mb-0">Total initiatives</p>
                </div>
            </div>
        </div>

        <!-- Pending -->
        <div class="col-md-3">
            <div class="card border-0 shadow-sm h-100 dash-card">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <div class="icon-box bg-warning bg-opacity-10 text-warning rounded-circle p-3">
                            <span class="material-icons">hourglass_empty</span>
                        </div>
                        <span class="badge bg-warning text-dark rounded-pill">Pending</span>
                    </div>
                    <h3 class="fw-bold mb-1">@ViewBag.PendingCount</h3>
                    <p class="text-muted small mb-0">Waiting for approval</p>
                </div>
            </div>
        </div>

        <!-- Approved -->
        <div class="col-md-3">
            <div class="card border-0 shadow-sm h-100 dash-card">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <div class="icon-box bg-success bg-opacity-10 text-success rounded-circle p-3">
                            <span class="material-icons">check_circle</span>
                        </div>
                        <span class="badge bg-success rounded-pill">Approved</span>
                    </div>
                    <h3 class="fw-bold mb-1">@ViewBag.ApprovedCount</h3>
                    <p class="text-muted small mb-0">Successful initiatives</p>
                </div>
            </div>
        </div>

        <!-- Rejected -->
        <div class="col-md-3">
            <div class="card border-0 shadow-sm h-100 dash-card">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <div class="icon-box bg-danger bg-opacity-10 text-danger rounded-circle p-3">
                            <span class="material-icons">cancel</span>
                        </div>
                        <span class="badge bg-danger rounded-pill">Declined</span>
                    </div>
                    <h3 class="fw-bold mb-1">@ViewBag.RejectedCount</h3>
                    <p class="text-muted small mb-0">Not approved</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Quick Actions -->
    <div class="mb-4">
        <h4 class="fw-bold text-dark mb-3">Quick Actions</h4>
        <div class="row g-4">
            <div class="col-md-4">
                <a asp-controller="Initiative" asp-action="History" class="text-decoration-none">
                    <div class="card border-0 text-white overflow-hidden shadow h-100" style="border-radius: 15px; min-height: 180px; background: linear-gradient(135deg, #1e3a8a, #0f172a);">
                        <div class="card-body d-flex flex-column align-items-center justify-content-center text-center">
                            <span class="material-icons mb-2 text-warning" style="font-size: 3rem;">history</span>
                            <h5 class="fw-bold mb-1">Submission History</h5>
                            <p class="small opacity-75">Review your initiatives</p>
                        </div>
                    </div>
                </a>
            </div>
            <div class="col-md-4">
                <a asp-controller="Home" asp-action="Privacy" asp-area="" class="text-decoration-none">
                    <div class="card border-0 text-white overflow-hidden shadow h-100" style="border-radius: 15px; min-height: 180px; background: linear-gradient(135deg, #14532d, #064e3b);">
                        <div class="card-body d-flex flex-column align-items-center justify-content-center text-center">
                            <span class="material-icons mb-2 text-warning" style="font-size: 3rem;">menu_book</span>
                            <h5 class="fw-bold mb-1">Guidelines</h5>
                            <p class="small opacity-75">Submission Process</p>
                        </div>
                    </div>
                </a>
            </div>
        </div>
    </div>
</div>
