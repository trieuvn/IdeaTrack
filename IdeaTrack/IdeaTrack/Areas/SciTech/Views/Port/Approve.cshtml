@using IdeaTrack.Models
@using IdeaTrack.Areas.SciTech.Models
@model InitiativeDetailVM

@{
    string statusBadgeClass = Model.Status switch
    {
        InitiativeStatus.Draft => "bg-secondary text-white",
        InitiativeStatus.Pending => "bg-warning text-dark",
        InitiativeStatus.Dept_Review => "bg-info text-white",
        InitiativeStatus.Reviewing => "bg-primary text-white",
        InitiativeStatus.Revision_Required => "bg-warning-subtle text-warning",
        InitiativeStatus.Approved => "bg-success text-white",
        InitiativeStatus.Rejected => "bg-danger text-white",
        _ => "bg-secondary text-white"
    };

    // Icon và màu cho Department
    string deptIcon = "fa-building";
    string deptColor = "text-secondary";

    switch (Model.DepartmentName.Replace("Khoa ", "").Trim())
    {
        case "Nông nghiệp":
            deptIcon = "fa-seedling"; deptColor = "text-success";
            break;
        case "Môi trường":
            deptIcon = "fa-leaf"; deptColor = "text-info";
            break;
        case "Công nghệ thông tin":
            deptIcon = "fa-laptop-code"; deptColor = "text-primary";
            break;
        case "Cơ khí":
            deptIcon = "fa-cogs"; deptColor = "text-warning";
            break;
        case "Điện - Điện tử":
            deptIcon = "fa-bolt"; deptColor = "text-danger";
            break;
        case "Hóa học":
            deptIcon = "fa-flask"; deptColor = "text-danger";
            break;
        case "Sinh học":
            deptIcon = "fa-dna"; deptColor = "text-success";
            break;
        case "Kinh tế":
            deptIcon = "fa-chart-line"; deptColor = "text-primary";
            break;
        case "Luật":
            deptIcon = "fa-gavel"; deptColor = "text-warning";
            break;
        case "Ngôn ngữ Anh":
            deptIcon = "fa-language"; deptColor = "text-info";
            break;
        case "Xây dựng":
            deptIcon = "fa-hard-hat"; deptColor = "text-secondary";
            break;
        case "Điều dưỡng":
            deptIcon = "fa-user-nurse"; deptColor = "text-danger";
            break;
        case "Quản trị Du lịch & Khách sạn":
            deptIcon = "fa-hotel"; deptColor = "text-warning";
            break;
        case "Truyền thông":
            deptIcon = "fa-bullhorn"; deptColor = "text-primary";
            break;
        case "Toán - Tin":
            deptIcon = "fa-square-root-alt"; deptColor = "text-info";
            break;
        case "Vật lý":
            deptIcon = "fa-atom"; deptColor = "text-info";
            break;
        case "Sinh học & Công nghệ":
            deptIcon = "fa-seedling"; deptColor = "text-success";
            break;
        case "Khoa học Xã hội & Nhân văn":
            deptIcon = "fa-users"; deptColor = "text-secondary";
            break;
        case "Giáo dục":
            deptIcon = "fa-chalkboard-teacher"; deptColor = "text-primary";
            break;
        case "Tài chính - Ngân hàng":
            deptIcon = "fa-piggy-bank"; deptColor = "text-success";
            break;
        case "Quản trị Kinh doanh":
            deptIcon = "fa-briefcase"; deptColor = "text-primary";
            break;
        case "Marketing":
            deptIcon = "fa-bullseye"; deptColor = "text-info";
            break;
        case "Du lịch & Lữ hành":
            deptIcon = "fa-route"; deptColor = "text-warning";
            break;
        case "Kiến trúc":
            deptIcon = "fa-drafting-compass"; deptColor = "text-danger";
            break;
        case "Dược":
            deptIcon = "fa-prescription-bottle-alt"; deptColor = "text-danger";
            break;
        case "Y học":
            deptIcon = "fa-stethoscope"; deptColor = "text-danger";
            break;
        case "Răng Hàm Mặt":
            deptIcon = "fa-tooth"; deptColor = "text-danger";
            break;
        case "Nhiệt đới học":
            deptIcon = "fa-temperature-high"; deptColor = "text-secondary";
            break;
        case "Khoa học Tự nhiên":
            deptIcon = "fa-flask"; deptColor = "text-secondary";
            break;
        case "Tâm lý học":
            deptIcon = "fa-brain"; deptColor = "text-info";
            break;
        case "Luật Quốc tế":
            deptIcon = "fa-balance-scale"; deptColor = "text-warning";
            break;
        default:
            deptIcon = "fa-building"; deptColor = "text-secondary";
            break;
    }
}

<!-- HEADER -->
<div class="d-flex flex-column flex-lg-row justify-content-between gap-3 mb-4">
    <div>
        <span class="badge @statusBadgeClass mb-2">
            <i class="fas fa-hourglass-half me-1"></i>@Model.Status
        </span>

        <h2 class="fw-bold mb-1">
            <i class="fas fa-lightbulb me-2 text-warning"></i>@Model.Title
        </h2>

        <p class="text-muted mb-0">
            <i class="fas fa-fingerprint me-1 text-info"></i>Mã hồ sơ: <strong>@Model.InitiativeCode</strong>
            <span class="mx-2">|</span>
            <i class="fas fa-calendar-alt me-1 text-secondary"></i>@Model.SubmittedDate.ToString("dd/MM/yyyy")
        </p>
    </div>
</div>

<div class="row g-4">

    <!-- LEFT COLUMN -->
    <div class="col-lg-8">

        <!-- INFO CARD -->
        <div class="card shadow-sm mb-4">
            <div class="card-header bg-light fw-bold">
                <i class="fas fa-info-circle me-1 text-primary"></i>Thông tin chung
            </div>

            <div class="card-body row g-4">
                <div class="col-md-6">
                    <small class="text-muted">Chủ nhiệm đề tài</small>
                    <p class="fw-semibold mb-0"><i class="fas fa-user-tie me-1 text-primary"></i>@Model.ProposerName</p>
                </div>

                <div class="col-md-6">
                    <small class="text-muted">Đơn vị thực hiện</small>
                    <p class="fw-semibold mb-0"><i class="fas @deptIcon @deptColor me-1"></i>@Model.DepartmentName</p>
                </div>

                <div class="col-md-6">
                    <small class="text-muted">Lĩnh vực</small>
                    <p class="fw-semibold mb-0"><i class="fas fa-brain me-1 text-info"></i>@Model.Category</p>
                </div>

                <div class="col-md-6">
                    <small class="text-muted d-block mb-1">Thời gian thực hiện</small>
                    <p class="fw-semibold mb-0 gradient-text">
                        <i class="fas fa-clock me-1"></i>
                        @Model.SubmittedDate.ToString("dd/MM/yyyy")
                    </p>
                </div>
                <div class="col-md-6">
                     <small class="text-muted">Kinh phí đề xuất</small>
                    <p class="fw-bold text-primary fs-5 mb-0"><i class="fas fa-dollar-sign me-1 text-success"></i>@Model.Budget.ToString("N0") VNĐ</p>
                </div>

            </div>
        </div>

        <!-- DESCRIPTION & FILES -->
        <div class="accordion mb-4" id="abstractAccordion">
            <div class="accordion-item">
                <h2 class="accordion-header">
                    <button class="accordion-button fw-semibold" data-bs-toggle="collapse" data-bs-target="#description">
                        <i class="fas fa-align-left me-2 text-primary"></i>Mô tả nội dung
                    </button>
                </h2>
                <div id="description" class="accordion-collapse collapse show">
                    <div class="accordion-body text-muted">
                        @Model.Description
                    </div>
                </div>
            </div>

            <div class="accordion-item">
                <h2 class="accordion-header">
                    <button class="accordion-button collapsed fw-semibold" data-bs-toggle="collapse" data-bs-target="#files">
                        <i class="fas fa-paperclip me-2 text-secondary"></i>Tài liệu đính kèm
                    </button>
                </h2>
                <div id="files" class="accordion-collapse collapse">
                    <div class="accordion-body">
                        <ul class="list-group list-group-flush">
                            @foreach (var file in Model.Files ?? new List<InitiativeFileVM>())
                            {
                                var ext = System.IO.Path.GetExtension(file.FileName).ToLower();
                                string iconClass = "fa-file";
                                string textColor = "text-secondary";

                                switch (ext)
                                {
                                    case ".pdf":
                                        iconClass = "fa-file-pdf";
                                        textColor = "text-danger";
                                        break;
                                    case ".xlsx":
                                    case ".xls":
                                        iconClass = "fa-file-excel";
                                        textColor = "text-success";
                                        break;
                                    case ".docx":
                                    case ".doc":
                                        iconClass = "fa-file-word";
                                        textColor = "text-primary";
                                        break;
                                    case ".pptx":
                                    case ".ppt":
                                        iconClass = "fa-file-powerpoint";
                                        textColor = "text-warning";
                                        break;
                                }

                                <li class="list-group-item d-flex justify-content-between">
                                    <span><i class="fas @iconClass @textColor me-2"></i>@file.FileName</span>
                                    <a href="@Url.Action("DownloadFile", "Port", new { fileName = file.FileName })" class="text-decoration-none">
                                        <i class="fas fa-download me-1 text-primary"></i>Tải
                                    </a>
                                </li>
                            }
                        </ul>
                    </div>
                </div>
            </div>
        </div>

    </div>

    <!-- RIGHT COLUMN -->
    <div class="col-lg-4">
        <div class="card shadow border-primary">
            <div class="card-header fw-bold text-primary">
                <i class="fas fa-pen-alt me-1"></i>Quyết định PKHCN
            </div>

            <div class="card-body">

                <!-- GHI CHÚ -->
                <label class="form-label fw-semibold">
                    <i class="fas fa-sticky-note me-1 text-warning"></i>
                    Ghi chú / Nội dung yêu cầu
                </label>

                <textarea id="requestContent"
                          class="form-control mb-3"
                          rows="4"
                          placeholder="Nhập nội dung yêu cầu chỉnh sửa hoặc kết luận...">
            </textarea>

                <!-- DEADLINE -->
                <label class="form-label fw-semibold">
                    <i class="fas fa-calendar-alt me-1 text-info"></i>
                    Hạn chỉnh sửa
                </label>

                <input type="date"
                       id="deadline"
                       class="form-control mb-3">

                <!-- DUYỆT -->
                <a href="/SciTech/Port/ApproveInitiative/@Model.Id" id="revisionLink2"
                   class="btn btn-success w-100 mb-2">
                    <i class="fas fa-check-circle me-1 text-white"></i>Duyệt hồ sơ
                </a>

                <!-- YÊU CẦU CHỈNH SỬA -->
                <a href="/SciTech/Port/RejectInitiative/@Model.Id"
                   id="revisionLink"
                   class="btn btn-outline-warning w-100 mb-2">
                    <i class="fas fa-undo me-1"></i>Yêu cầu chỉnh sửa
                </a>

            </div>
        </div>
    </div>


    <script>
        document.getElementById("revisionLink").addEventListener("click", function (e) {
            e.preventDefault(); 

            const content = document.getElementById("requestContent").value;
            const deadline = document.getElementById("deadline").value;

            if (!content || !deadline) {
                alert("Vui lòng nhập nội dung và hạn chỉnh sửa!");
                return;
            }

            const baseUrl = this.getAttribute("href");
            const url = baseUrl
                + "?requestContent=" + encodeURIComponent(content)
                + "&deadline=" + deadline;

            window.location.href = url;
        });
         document.getElementById("revisionLink2").addEventListener("click", function (e) {
            e.preventDefault();

           const content = document.getElementById("requestContent").value;
            if (!content) {
                alert("Vui lòng nhập nội dung !");
                return;
            }

            const baseUrl = this.getAttribute("href");
            const url = baseUrl
                + "?requestContent=" + encodeURIComponent(content);

            window.location.href = url;
        });
    </script>


</div>
