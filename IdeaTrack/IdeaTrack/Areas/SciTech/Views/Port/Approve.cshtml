@using IdeaTrack.Models
@using IdeaTrack.Areas.SciTech.Models
@model InitiativeDetailVM

@{
    string statusBadgeClass = Model.Status switch
    {
        InitiativeStatus.Draft => "bg-secondary text-white",
        InitiativeStatus.Pending => "bg-warning text-dark",
        InitiativeStatus.Faculty_Approved => "bg-info text-white",
        InitiativeStatus.Evaluating => "bg-primary text-white",
        InitiativeStatus.Revision_Required => "bg-warning-subtle text-warning",
        InitiativeStatus.Approved => "bg-success text-white",
        InitiativeStatus.Rejected => "bg-danger text-white",
        _ => "bg-secondary text-white"
    };

    // Icon v� m�u cho Department
    string deptIcon = "fa-building";
    string deptColor = "text-secondary";

    switch (Model.Code)
    {
        case "CNTT":
            deptIcon = "fa-laptop-code"; deptColor = "text-primary";
            break;
        case "DTVT": // �i?n t? Vi?n th�ng
            deptIcon = "fa-bolt"; deptColor = "text-danger";
            break;
        case "KHCN": // Khoa h?c C�ng ngh?
            deptIcon = "fa-microscope"; deptColor = "text-success";
            break;
        case "CK": // Co kh�
            deptIcon = "fa-cogs"; deptColor = "text-warning";
            break;
        case "XD": // X�y d?ng
            deptIcon = "fa-hard-hat"; deptColor = "text-secondary";
            break;
        case "MT": // M�i tru?ng
            deptIcon = "fa-leaf"; deptColor = "text-info";
            break;
        case "KT": // Kinh t?
            deptIcon = "fa-chart-line"; deptColor = "text-primary";
            break;
        case "QTKD": // Qu?n tr? Kinh doanh
            deptIcon = "fa-briefcase"; deptColor = "text-primary";
            break;
        case "LUAT":
            deptIcon = "fa-gavel"; deptColor = "text-warning";
            break;
        case "NN": // N�ng nghi?p
            deptIcon = "fa-seedling"; deptColor = "text-success";
            break;
        default:
            deptIcon = "fa-building"; deptColor = "text-secondary";
            break;
    }
}

<!-- HEADER -->
<div class="d-flex flex-column flex-lg-row justify-content-between gap-3 mb-4">
    <div>
        <span class="badge @statusBadgeClass mb-2">
            <i class="fas fa-hourglass-half me-1"></i>@Model.Status
        </span>

        <h2 class="fw-bold mb-1">
            <i class="fas fa-lightbulb me-2 text-warning"></i>@Model.Title
        </h2>

        <p class="text-muted mb-0">
            <i class="fas fa-fingerprint me-1 text-info"></i>M� h? so: <strong>@Model.InitiativeCode</strong>
            <span class="mx-2">|</span>
            <i class="fas fa-calendar-alt me-1 text-secondary"></i>@Model.SubmittedDate.ToString("dd/MM/yyyy")
        </p>
    </div>
</div>

<div class="row g-4">

    <!-- LEFT COLUMN -->
    <div class="col-lg-8">

        <!-- INFO CARD -->
        <div class="card shadow-sm mb-4">
            <div class="card-header bg-light fw-bold">
                <i class="fas fa-info-circle me-1 text-primary"></i>Th�ng tin chung
            </div>

            <div class="card-body row g-4">
                <div class="col-md-6">
                    <small class="text-muted">Ch? nhi?m d? t�i</small>
                    <p class="fw-semibold mb-0"><i class="fas fa-user-tie me-1 text-primary"></i>@Model.ProposerName</p>
                </div>

                <div class="col-md-6">
                    <small class="text-muted">�on v? th?c hi?n</small>
                    <p class="fw-semibold mb-0"><i class="fas @deptIcon @deptColor me-1"></i>@Model.DepartmentName</p>
                </div>

                <div class="col-md-6">
                    <small class="text-muted">Linh v?c</small>
                    <p class="fw-semibold mb-0"><i class="fas fa-brain me-1 text-info"></i>@Model.Category</p>
                </div>

                <div class="col-md-6">
                    <small class="text-muted d-block mb-1">Th?i gian th?c hi?n</small>
                    <p class="fw-semibold mb-0 gradient-text">
                        <i class="fas fa-clock me-1"></i>
                        @Model.SubmittedDate.ToString("dd/MM/yyyy")
                    </p>
                </div>
                <div class="col-md-6">
                     <small class="text-muted">Kinh ph� d? xu?t</small>
                    <p class="fw-bold text-primary fs-5 mb-0"><i class="fas fa-dollar-sign me-1 text-success"></i>@Model.Budget.ToString("N0") VN�</p>
                </div>

            </div>
        </div>

        <!-- DESCRIPTION & FILES -->
        <div class="accordion mb-4" id="abstractAccordion">
            <div class="accordion-item">
                <h2 class="accordion-header">
                    <button class="accordion-button fw-semibold" data-bs-toggle="collapse" data-bs-target="#description">
                        <i class="fas fa-align-left me-2 text-primary"></i>M� t? n?i dung
                    </button>
                </h2>
                <div id="description" class="accordion-collapse collapse show">
                    <div class="accordion-body text-muted">
                        @Model.Description
                    </div>
                </div>
            </div>

            <div class="accordion-item">
                <h2 class="accordion-header">
                    <button class="accordion-button collapsed fw-semibold" data-bs-toggle="collapse" data-bs-target="#files">
                        <i class="fas fa-paperclip me-2 text-secondary"></i>T�i li?u d�nh k�m
                    </button>
                </h2>
                <div id="files" class="accordion-collapse collapse">
                    <div class="accordion-body">
                        <ul class="list-group list-group-flush">
                            @foreach (var file in Model.Files ?? new List<InitiativeFileVM>())
                            {
                                var ext = System.IO.Path.GetExtension(file.FileName).ToLower();
                                string iconClass = "fa-file";
                                string textColor = "text-secondary";

                                switch (ext)
                                {
                                    case ".pdf":
                                        iconClass = "fa-file-pdf";
                                        textColor = "text-danger";
                                        break;
                                    case ".xlsx":
                                    case ".xls":
                                        iconClass = "fa-file-excel";
                                        textColor = "text-success";
                                        break;
                                    case ".docx":
                                    case ".doc":
                                        iconClass = "fa-file-word";
                                        textColor = "text-primary";
                                        break;
                                    case ".pptx":
                                    case ".ppt":
                                        iconClass = "fa-file-powerpoint";
                                        textColor = "text-warning";
                                        break;
                                }

                                <li class="list-group-item d-flex justify-content-between">
                                    <span><i class="fas @iconClass @textColor me-2"></i>@file.FileName</span>
                                    <a href="@Url.Action("DownloadFile", "Port", new { fileName = file.FileName })" class="text-decoration-none">
                                        <i class="fas fa-download me-1 text-primary"></i>T?i
                                    </a>
                                </li>
                            }
                        </ul>
                    </div>
                </div>
            </div>
        </div>

    </div>

    <!-- RIGHT COLUMN -->
    <div class="col-lg-4">
        <div class="card shadow border-primary">
            <div class="card-header fw-bold text-primary">
                <i class="fas fa-pen-alt me-1"></i>Quy?t d?nh PKHCN
            </div>

            <div class="card-body">

                <!-- GHI CH� -->
                <label class="form-label fw-semibold">
                    <i class="fas fa-sticky-note me-1 text-warning"></i>
                    Ghi ch� / N?i dung y�u c?u
                </label>

                <textarea id="requestContent"
                          class="form-control mb-3"
                          rows="4"
                          placeholder="Nh?p n?i dung y�u c?u ch?nh s?a ho?c k?t lu?n..."></textarea>

                <!-- DEADLINE -->
                <div id="deadlineWrapper" class="d-none">
                    <label class="form-label fw-semibold">
                        <i class="fas fa-calendar-alt me-1 text-info"></i>
                        H?n ch?nh s?a
                    </label>

                    <input type="date"
                           id="deadline"
                           class="form-control mb-3">
                </div>

                <!-- DUY?T -->
                <a href="/SciTech/Port/ApproveInitiative/@Model.Id" id="revisionLink2"
                   class="btn btn-success w-100 mb-2">
                    <i class="fas fa-check-circle me-1 text-white"></i>Duy?t h? so
                </a>

                <!-- Y�U C?U CH?NH S?A -->
                <a href="/SciTech/Port/RejectInitiative/@Model.Id"
                   id="revisionLink"
                   class="btn btn-outline-warning w-100 mb-2">
                    <i class="fas fa-undo me-1"></i>Y�u c?u ch?nh s?a
                </a>

            </div>
        </div>
    </div>



</div>
<script>
    document.getElementById("revisionLink").addEventListener("click", function (e) {
        e.preventDefault();

        const deadlineWrapper = document.getElementById("deadlineWrapper");
        const content = document.getElementById("requestContent").value;
        const deadlineInput = document.getElementById("deadline");

        // N?u deadline dang ?n ? hi?n l�n v� d?ng l?i
        if (deadlineWrapper.classList.contains("d-none")) {
            deadlineWrapper.classList.remove("d-none");
            deadlineInput.focus();
            return;
        }

        const deadline = deadlineInput.value;

        if (!content || !deadline) {
            alert("Vui l�ng nh?p n?i dung v� h?n ch?nh s?a!");
            return;
        }

        const baseUrl = this.getAttribute("href");
        const url = baseUrl
            + "?requestContent=" + encodeURIComponent(content)
            + "&deadline=" + deadline;

        window.location.href = url;
    });

    document.getElementById("revisionLink2").addEventListener("click", function (e) {
        e.preventDefault();

        const content = document.getElementById("requestContent").value;

        if (!content) {
            alert("Vui l�ng nh?p n?i dung!");
            return;
        }

        const baseUrl = this.getAttribute("href");
        const url = baseUrl
            + "?requestContent=" + encodeURIComponent(content);

        window.location.href = url;
    });
</script>

