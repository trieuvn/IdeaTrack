@model IdeaTrack.Areas.SciTech.Models.PeriodListVM

<main class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="fw-bold mb-1">
                <span class="material-icons align-middle me-2 text-primary">date_range</span>
                Initiative Period Management
            </h2>
            <p class="text-muted mb-0">Open/close submission periods and manage per-period configuration</p>
        </div>
        <a asp-action="Create" asp-controller="Period" asp-area="SciTech" class="btn btn-primary rounded-pill px-4">
            <span class="material-icons align-middle me-1" style="font-size: 18px;">add</span> Create New Period
        </a>
    </div>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }
    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            @TempData["ErrorMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <div class="card card-custom shadow-sm">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover mb-0">
                    <thead class="table-light">
                        <tr>
                            <th class="ps-4">Period Name</th>
                            <th>Academic Year</th>
                            <th>Duration</th>
                            <th class="text-center">Categories</th>
                            <th class="text-center">Initiatives</th>
                            <th class="text-center">Status</th>
                            <th class="text-end pe-4">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model.Items.Any())
                        {
                            @foreach (var item in Model.Items)
                            {
                                <tr>
                                    <td class="ps-4">
                                        <strong>@item.Name</strong>
                                    </td>
                                    <td>@item.AcademicYearName</td>
                                    <td>
                                        <small>@item.StartDate.ToString("dd/MM/yyyy") - @item.EndDate.ToString("dd/MM/yyyy")</small>
                                    </td>
                                    <td class="text-center">
                                        <a asp-controller="Category" asp-action="Index" asp-route-periodId="@item.Id" class="badge bg-info text-decoration-none">
                                            @item.CategoryCount categories
                                        </a>
                                    </td>
                                    <td class="text-center">
                                        <span class="badge bg-secondary">@item.InitiativeCount</span>
                                    </td>
                                    <td class="text-center">
                                        @if (item.IsOpen)
                                        {
                                            <span class="badge bg-success">
                                                <i class="fas fa-door-open me-1"></i>Open
                                            </span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-outline-secondary text-muted border">
                                                <i class="fas fa-door-closed me-1"></i>Closed
                                            </span>
                                        }
                                    </td>
                                    <td class="text-end pe-4">
                                        <div class="btn-group">
                                            <form asp-action="ToggleActive" asp-route-id="@item.Id" method="post" class="d-inline">
                                                @Html.AntiForgeryToken()
                                                @if (item.IsActive)
                                                {
                                                    <button type="submit" class="btn btn-sm btn-outline-warning">
                                                        <span class="material-icons" style="font-size: 16px;">lock</span>
                                                    </button>
                                                }
                                                else
                                                {
                                                    <button type="submit" class="btn btn-sm btn-outline-success">
                                                        <span class="material-icons" style="font-size: 16px;">lock_open</span>
                                                    </button>
                                                }
                                            </form>
                                            <a asp-action="Edit" asp-controller="Period" asp-area="SciTech" asp-route-id="@item.Id" class="btn btn-sm btn-outline-primary">
                                                <span class="material-icons" style="font-size: 16px;">edit</span>
                                            </a>
                                            @if (item.InitiativeCount == 0)
                                            {
                                                <form asp-action="Delete" asp-route-id="@item.Id" method="post" class="d-inline"
                                                      onsubmit="return confirm('Are you sure you want to delete this period?');">
                                                    @Html.AntiForgeryToken()
                                                    <button type="submit" class="btn btn-sm btn-outline-danger">
                                                        <span class="material-icons" style="font-size: 16px;">delete</span>
                                                    </button>
                                                </form>
                                            }
                                        </div>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="7" class="text-center py-5 text-muted">
                                    <span class="material-icons mb-2" style="font-size: 48px;">inbox</span>
                                    <p class="mb-0">No initiative periods found.</p>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    @if (Model.TotalPages > 1)
    {
        <nav class="mt-4">
            <ul class="pagination justify-content-center">
                @for (int i = 1; i <= Model.TotalPages; i++)
                {
                    <li class="page-item @(i == Model.CurrentPage ? "active" : "")">
                        <a class="page-link" asp-action="Index" asp-route-page="@i">@i</a>
                    </li>
                }
            </ul>
        </nav>
    }
</main>
